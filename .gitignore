target/
/fuzz/target
/fuzz/Cargo.lock
# Reference specs / test media are NOT redistributed (copyright); only the
# provenance manifest (specs/README.md) is tracked. See it for sources + hashes.
/specs/*
!/specs/README.md
